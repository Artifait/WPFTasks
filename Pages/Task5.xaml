<Page x:Class="WPFTasks.Pages.Task5"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFTasks.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Task5">

    <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}"
            BorderBrush="Gray" BorderThickness="2" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" x:Name="AuthorTextBox" Margin="0,0,10,0"
                         ToolTip="Введите имя и фамилию автора..." Style="{DynamicResource TextBoxStyle}" />
                <Button Grid.Column="1" Content="Скачать книги" Style="{DynamicResource DarkButtonStyle}"
                        Click="DownloadButton_Click" />
            </Grid>

            <!-- Статус -->
            <TextBlock x:Name="StatusTextBlock" Grid.Row="1" VerticalAlignment="Top"
                       Text="Статус: Ожидание..." FontSize="14" Foreground="Gray" />
        </Grid>
    </Border>
</Page>
