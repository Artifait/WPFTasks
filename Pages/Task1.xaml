<Page x:Class="WPFTasks.Pages.Task1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFTasks.Pages"
      mc:Ignorable="d"      
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Task1">
    <Page.Resources>
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
        </Style>
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="DarkSlateGray"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="NotificationTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>
    </Page.Resources>

    <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}"
            BorderBrush="Gray" BorderThickness="2">
        <Grid Margin="10">
            <!-- Основное разбиение на строки и колонки -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Ввод текста -->
            <TextBlock Text="Введите текст:" 
                   Style="{StaticResource LabelTextStyle}" 
                   Grid.Row="0" Grid.Column="0" 
                   Margin="0,0,0,5"/>
            <TextBox x:Name="InputTextBox" 
                 Style="{StaticResource TextBoxStyle}" 
                 Grid.Row="1" Grid.Column="0" 
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                 AcceptsReturn="True" 
                 TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Visible" />

            <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                <!-- Панель выбора опций -->
                <CheckBox Content="Количество предложений" 
                      Style="{StaticResource CheckBoxStyle}" 
                      IsChecked="{Binding IsSentenceCountChecked}" />
                <CheckBox Content="Количество символов" 
                      Style="{StaticResource CheckBoxStyle}" 
                      IsChecked="{Binding IsCharCountChecked}" />
                <CheckBox Content="Количество слов" 
                      Style="{StaticResource CheckBoxStyle}" 
                      IsChecked="{Binding IsWordCountChecked}" />
                <CheckBox Content="Вопросительные предложения" 
                      Style="{StaticResource CheckBoxStyle}" 
                      IsChecked="{Binding IsQuestionCountChecked}" />
                <CheckBox Content="Восклицательные предложения" 
                      Style="{StaticResource CheckBoxStyle}" 
                      IsChecked="{Binding IsExclamationCountChecked}" />

                <!-- Кнопки управления -->
                <Button Content="Анализировать" 
                    Style="{StaticResource DarkButtonStyle}" 
                    Command="{Binding AnalyzeCommand}"
                    HorizontalAlignment="Left" Margin="20,10,0,0" />
                <Button Content="Остановить" 
                    Style="{StaticResource DarkButtonStyle}" 
                    Command="{Binding StopCommand}"
                    HorizontalAlignment="Left" Margin="20, 10, 0, 10"/>
                <Button Content="Сохранить отчёт"
                    Style="{StaticResource DarkButtonStyle}" 
                    HorizontalAlignment="Left" Margin="20, 0, 0, 0" 
                    Command="{Binding SaveReportCommand}" />
            </StackPanel>

            <!-- Уведомления об операциях -->
            <TextBlock x:Name="NotificationTextBlock" 
                   Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                   Style="{StaticResource NotificationTextStyle}" 
                   Text="{Binding NotificationMessage}" />
        </Grid>
    </Border>
</Page>
