<Page x:Class="WPFTasks.Pages.Task1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"      
      d:DesignHeight="500" d:DesignWidth="850"
      Title="Task1">
    <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}"
        BorderBrush="Gray" BorderThickness="2" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock x:Name="LoadingView" Text="Загрузка текста: Гамлет" 
                       Style="{StaticResource LabelTextStyle}" 
                       HorizontalAlignment="Center" 
                       Margin="0 0 0 10"/>

            <!-- Текст -->
            <TextBox x:Name="HamletText" Grid.Row="1" Text="{Binding HamletText, UpdateSourceTrigger=PropertyChanged}" Foreground="Wheat" VerticalScrollBarVisibility="Auto"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsReadOnly="True" TextWrapping="Wrap"
                        Style="{StaticResource TextBoxStyle}"/>

            <!-- Кнопка загрузки -->
            <Button Grid.Row="2" Content="Загрузить текст"
                    Command="{Binding LoadHamletCommand}" Click="OnClick"
                    Style="{StaticResource DarkButtonStyle}" 
                    HorizontalAlignment="Center" Margin="0 10 0 0"/>
        </Grid>
    </Border>
</Page>
